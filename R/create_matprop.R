#' Create material property list
#' 
#' Generate a list of parameters that describes a material that will be subject to evaporation calculations.
#
#' @values Takes no input but initializes the work space.
#
#' @author Peter Norgard
create_matprop = function()
{
	property_list = list();
	return(property_list);
}

